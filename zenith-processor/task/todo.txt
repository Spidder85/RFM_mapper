TODO для модуля zenith-processor

[?] 1. Должно быть 2-3 режима работы:
   - только загружает реестр, полученный по событию из rfm, и создает событие в указанных папках офисов;
   - только запускает массовую проверку по событию после импорта;
   - полный цикл.
   Статус: режимы FULL / IMPORT_ONLY / CHECK_ONLY реализованы технически.
   Осталось: проверить на реальном Zenith.

[?] 2. После проверки должен выгружать соответствующий отчет и анализировать его.
   Статус: код создания/скачивания/анализа отчета есть.
   Осталось: проверить на реальном Zenith для te21/un/mvk и реальных фильтров.

[x] 3. БД найденных лиц должна указывать, в каком перечне человек был найден.
   Статус: реализовано через catalog в FoundPersonsStore/StoredPerson.

[ ] 4. После проверки при необходимости должен отправлять уведомление.
   Статус: НЕ реализовано в zenith-processor.
   Сейчас email/telegram отправляет только rfm-downloader.
   Нужно отдельной доработкой вынести/переиспользовать уведомления и вызывать их после processCheckOnly/processFull.

[x] 5. Конфиг Zenith не должен быть избыточным для пользователя.
   Статус: template упрощен: убраны FileFormat, Append, Periodic, Storage из пользовательской части.

[?] 6. Import должен выбирать file_format по типу реестра, а не из конфига.
   Статус: ZenithImportFormatResolver реализован.
   Осталось: проверить реальные вызовы API:
     te21 -> TerroristsXml
     un   -> UnXml + list_category
     mvk  -> CftXml + list_category

[?] 7. Events для офисной схемы:
   центральный Zenith после импорта должен по списку папок создать события для клиентских частей.
   Статус: ImportCompletedDirectories и ZenithImportEventPublisher реализованы.
   Осталось: проверить запись в реальные сетевые папки офисов.

2026.07.08

[ ] 8. При полном цикле FULL событие ZenithImportCompleted для офисов тоже должно формироваться.
   Статус: сейчас в processFull генерация события закомментирована.
   Нужно: раскомментировать/добавить публикацию после успешного импорта и до массовой проверки.
