* общий TODO для проекта: по отдельности для проектов (rfm-downloader и zenith-processor) в каждом проекте соответственно в папке task файл todo.txt *

Обозначения:
[x] - реализовано и сомнений в работоспособности по текущему коду/проверке нет
[?] - технически реализовано, но еще требует проверки на реальном сценарии
[ ] - не реализовано или требует доработки

[?] продумать возможность обработки ошибочных events/failed
    Статус: технически есть обработка failed/retry для файловых событий, но полный сценарий Zenith еще не проверен.

2026.07.08

[x] 1. При загрузке нескольких каталогов формируется несколько писем, а нужно одно итоговое уведомление за запуск.

2026.07.09
[x] мне кажется сейчас формирование уведомление избыточно (оно дублируется в коде для общего запуска и отдельно для зенита). Думаю часть уведомления зенитанеобходимо формировать в одном месте, например в в блоке зенита, а rfm его просто вызывает чтобы дополнить основной.

[x] надо предусмотреть чтобы сообщения не отправлялись дважды. Например если и у rfm и у zenith в config включены Notification, то уйдут 2 письма, а не одно. Надо чтобы в модуле зенита отправлялось уведомление только если не отправляется основное (если оба включены тоотправляется только основное, зенит выключается, если одно из включено, то срабатывает то которое включено). Как вариант основной должен насильно выключать для Zenith или для Zenith просто игнорироватья если основной включен

[x] лог работы зенита не архивируется. он просто дописал дату к имени файла "zenith-processor.2026-07-07.0.log" и начал новый файл "zenith-processor.log"


2026.07.13
[] уведомление от зенит в режиме check_only (когда зенит запускается сам) отправляются от каждого перечня по отдельности, а должны суммартно как и при основном (когда зенит запускается от rfm_downloader)

2026.07.16
[] необходимо сделать чтобы зенит мог отправлять сообщение что реетр был обновлен и загружен если он запускается отдельно от rfm

2026.07.17
[] если при обработки сервер зенит не доступен, то есть 2 варианта (не знаю какой ллучше)
    1) не помечать обработку как ошибочную, а оставлять событие как новое и при следующем запуске он попробует ее обработать еще раз
    2) помечать как ошибочную, но всеравно при следующем запуске еще раз обрабатывать его

[x] не знаю в функционал какого модуля добавить, но необхдимо если накопилось несколько обновлений одного списка брать только последний (например если накопилось 2 (или более) обновленных списков террористов то проверку должен осуществлять по последнему списку), а предыдущие события удалять

2026.07.21
[] не понятно как будет зенит чистить сбытия, если в нем нет методов retention (по крайней мере я не вижу) или они будут копиться бесконечно?

[] как я уже писал в todo rfm надо чтобы по умолчанию загрузки хранились вечно. но это не касается событий. события должны чиститьс и у rfm и у zenith, а не копиться вечно. Архивы логов также должнычиститься по истечению времени. Возможно что то из этого уже реализовано