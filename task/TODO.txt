* общий TODO для проекта: по отдельности для проектов (rfm-downloader и zenith-processor) в каждом проекте сответветсвенно в папке task файл todo.txt*
? - эти пункты выполнены но еще не проверены

[?] продумать возможность обработки ошибочных events/failed

[?] скорее всего надо events както бекапить и со врменем (или по обьему) чистить, иначе, как я полагаю, они будут копиться бесконечно и будут забивать память

[?] log запуска зеита скорее всего не ведется. уведомление ссылается на лог "Проверьте журнал zenith-processor", но его нигде нет

[ ] модуль zenith-processor должен иметь возможность автономного запуска

[ ] продумать архитектуру работы. Смысл: РФМ мониторит 3 реестра, после обновления создает событие (или апускает зенит) что реестр был обновлен. Далее запусается зенит (либо он крутиться как служба, либо его запускает rfm (наверное самый правильный вариант), либо по расписанию в планировщике). у зенита должен быть разделен функционал (через настройки): 1 - он только загружает загруженный реестр (1 из 3) в себя; 2 - он только запускает массовую проверку, 3 - он загружает и запускает проверку. После проверки он выгружает отчет по задданым критериям для соответсвующего перечная и проверяет

Вопрос как лучше реализовать работу той части зенита которая будет обрабатывать по событию (будет запускаться сам ез rfm)