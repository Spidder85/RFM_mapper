* общий TODO для проекта: по отдельности для проектов (rfm-downloader и zenith-processor) в каждом проекте соответственно в папке task файл todo.txt *

Обозначения:
[x] - реализовано и сомнений в работоспособности по текущему коду/проверке нет
[?] - технически реализовано, но еще требует проверки на реальном сценарии
[ ] - не реализовано или требует доработки

[?] продумать возможность обработки ошибочных events/failed
    Статус: технически есть обработка failed/retry для файловых событий, но полный сценарий Zenith еще не проверен.

[?] events нужно чистить по сроку/объему, иначе они будут копиться.
    Статус: для events/registry-updated есть EventRetentionService.
    Осталось: отдельно проверить/добавить очистку офисных очередей ZenithImportCompleted для клиентских папок.

[x] log запуска Zenith ведется.
    Статус: logback.xml в zenith-processor есть, логирование настроено.

[?] модуль zenith-processor должен иметь возможность автономного запуска.
    Статус: есть run-zenith-once.bat, run-zenith-drain.bat, run-zenith-watch.bat и режимы once/drain/watch.
    Осталось: проверить на реальном Zenith и офисных сетевых папках.

[?] архитектура работы через события и режимы Zenith.
    Статус: режимы FULL / IMPORT_ONLY / CHECK_ONLY реализованы технически.
    Осталось: проверить сценарии:
      1. rfm-downloader создает RegistryUpdated;
      2. центральный Zenith импортирует реестр;
      3. центральный Zenith создает ZenithImportCompleted в папки офисов;
      4. офисный Zenith запускает массовую проверку по своему событию.

!!![ ] лог периодически не архивируется и после полуночи продолжает писаться в старый файл.
    Статус: проблема периодическая.
    Сначала проверить в планировщике запрет параллельных запусков: "Не запускать новый экземпляр".
    Если повторится - менять схему logback на датированный активный файл.

2026.07.08

[ ] 1. При загрузке нескольких каталогов формируется несколько писем, а нужно одно итоговое уведомление за запуск.
